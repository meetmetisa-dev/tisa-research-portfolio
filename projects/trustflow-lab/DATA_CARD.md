# Data card — TrustFlow Lab

The committed fixture is synthetic and time ordered. It contains aggregate failed-authentication counts, unique-port counts, packet rate, outbound ratio, a synthetic host group and a binary label.

Real network data requires data-owner authorization, identifier minimization, retention controls and an audit for duplicated, generated or trivially leaked label features. Host identities must not cross train/test boundaries without an explicit generalization rationale.
