# Reproducibility

`make demos` records the seed, time split, threshold, alert count, precision, recall, false-positive rate and benign drift. `make test` checks metric bounds and drift output type.

For real adapters, hash source files, document license and preprocessing, preserve chronological order and publish the exact host/time grouping rule.
