# Double DQN for Drift-Aware QoE Recovery

**Topic:** Deep Reinforcement Learning  
**Author:** Tisa Selma, Ph.D.  
**Status:** Reproducible synthetic research prototype - not a peer-reviewed publication or production system.

## Portfolio purpose

A NumPy Double-DQN controller selects monitoring and recovery actions from throughput, buffer, residual, drift, and cooldown state.

This project is designed to demonstrate research reasoning, implementation quality, reproducibility, baseline discipline, and transparent limitations. It extends Tisa Selma's documented work in adaptive, explainable, multimodal, and drift-aware AI without representing the prototype as previously published evidence.

## Research question

Can a learned controller reduce outages and residual error while managing the cost and fatigue of recovery actions under changing operating conditions?

## Method

A five-input ReLU Q-network uses experience replay, online and target networks, and Double-DQN targets. The policy is evaluated against monitor-only, random, and threshold baselines.

## Shipped deterministic results

| Metric | Value |
|---|---:|
| mean simulated QoE | 0.800 |
| outage rate | 0.05% |
| mean residual | 0.112 |
| reward gain vs monitor-only | +648.7% |

The complete machine-readable results are in [`results.json`](results.json). All values are simulation outputs generated by the included code.

## Reproduce

```bash
python -m pip install -r requirements.txt
python demo.py --check --output results.json
python validate.py
```

## Repository contents

- `demo.py` - executable experiment
- `results.json` - shipped deterministic output
- `RESEARCH_PACKAGE.md` - combined research protocol, data card, and model card
- `validate.py` - deterministic reproduction and sanity checks
- `index.html` and `project.css` - standalone portfolio page

## Scientific contribution

The project connects model monitoring to sequential intervention, reports action distributions and operational outcomes, and preserves competitive non-learning baselines for interpretation.

## Limitations

The environment is synthetic and the controller is educational. Results are deterministic simulation outputs, not production performance or safety certification.

## Evidence boundary

The project is a portfolio prototype created in August 2026. It must not be described as a peer-reviewed result, a deployed system, a privacy guarantee, a clinical result, or evidence collected from real users. Any future empirical claim should be tied to a named dataset, versioned code, an approved evaluation protocol, and appropriate ethics or governance review.
