# Reproducibility

Run `make test && make demos`. The default seed is `2026`. The report records the exact public metadata fields and asserts `payload_bytes_inspected: 0`.

For a full network testbed, record origin/player versions, network profile, capture filter, reduction code, protocol version, hardware and wall-clock timing.
